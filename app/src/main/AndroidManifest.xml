<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loginpage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginPage">
        <activity
            android:name="drivers.ProfileActivity"
            android:exported="false" />
        <activity
            android:name="drivers.MealGeneratorActivity"
            android:exported="false" />
        <activity
            android:name="drivers.HomePageActivity"
            android:exported="false" />
        <activity
            android:name="drivers.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="drivers.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>